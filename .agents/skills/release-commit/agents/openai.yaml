interface:
  display_name: "Release Commit"
  short_description: "Cut an BerryKeep release commit and tag"
  default_prompt: "Use $release-commit to prepare and publish the next BerryKeep release commit and tag."
